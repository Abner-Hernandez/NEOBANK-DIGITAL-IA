# --- Entornos y secretos ---
.env
*.env
!.env.example

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
env/
.pytest_cache/

# --- Vector store (se regenera con `python -m scripts.build_index`, no se versiona) ---
vectorstore/
faiss_index/
chroma_db/

# --- Logs (el contenido no se versiona; la carpeta se preserva con .gitkeep) ---
logs/*.log

# --- Sistema operativo / IDE ---
.DS_Store
.vscode/
.idea/

# --- Docker (para fases posteriores) ---
*.pid